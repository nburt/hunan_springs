<div class="menu-types-container">
  <h1>Menu Types</h1>

  <%= flash[:menu_type_success] %>

  <%= link_to "Add Menu Type", new_admin_menu_type_path %>

  <table>
    <thead>
    <th>Name</th>
    </thead>
    <tbody>

    <% @menu_types.each do |menu_type| %>
      <tr>
        <td><%= menu_type.name %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
